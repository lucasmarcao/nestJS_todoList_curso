import { Injectable } from '@nestjs/common';

@Injectable()
export class AppService {

  getHello(): string {
    // let verifica:string = String(document.location.pathname);
    // console.log(verifica);
    return ' yyy MARCAO xd xd xd';
    // npm run start
    // o pra desenvolvedor --> npm run start:dev
  }
}
